streamlit
python-dotenv
groq

# Numpy pinned first — must be installed before thinc/spacy build against it
numpy==1.26.4

# spaCy stack — pinned to versions that are mutually compatible on Python 3.11
spacy==3.7.4
thinc==8.2.3
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# Unstructured — pinned to version that works with spacy 3.7.x
unstructured==0.12.5
unstructured[pdf]==0.12.5
pdfminer.six
pdfplumber
pillow
python-magic

# LlamaIndex
llama-index
llama-index-embeddings-huggingface
llama-index-vector-stores-chroma

# Retrieval
chromadb
rank_bm25
sentence-transformers

# Torch CPU only — avoids the 2GB CUDA build
torch --index-url https://download.pytorch.org/whl/cpu